/**
 * @internal
 */
export const DEFAULT_IPFS_GATEWAY = "https://cloudflare-ipfs.com/ipfs/";

/**
 * @internal
 */
export const TW_IPFS_SERVER_URL = "https://ipfs.web3sdks.com";

/**
 * @internal
 */
export const PINATA_IPFS_URL = `https://api.pinata.cloud/pinning/pinFileToIPFS`;

/**
 * @internal
 */
export const WEB3SDKS_URL = `https://web3sdks.com`;

export const pkg = require("../../package.json");

export const cliVersion = pkg.version;
